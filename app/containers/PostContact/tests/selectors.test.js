// import { fromJS } from 'immutable';
// import { makeSelectPostContactDomain } from '../selectors';

// const selector = makeSelectPostContactDomain();

describe('makeSelectPostContactDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
