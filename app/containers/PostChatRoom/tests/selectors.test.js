// import { fromJS } from 'immutable';
// import { makeSelectPostChatRoomDomain } from '../selectors';

// const selector = makeSelectPostChatRoomDomain();

describe('makeSelectPostChatRoomDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
