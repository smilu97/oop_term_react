// import { fromJS } from 'immutable';
// import { makeSelectChatDomain } from '../selectors';

// const selector = makeSelectChatDomain();

describe('makeSelectChatDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
